version: "3.7"

services:
  app:
    build:
      context: ./app
      dockerfile: Dockerfile
    env_file:
      - env/.app.env
    ports:
      - "5000:5000"
    volumes:
      - ./app:/home/python/app
